apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.github.tokenSecretName }}
  namespace: {{ .Values.squad.namespace }}
  labels:
    {{- include "squad.labels" . | nindent 4 }}
type: Opaque
stringData:
  token: {{ .Values.github.token | default "REPLACE_WITH_GITHUB_PAT" }}
