# Test Document

This is a simple test document for the podcaster prototype.

## Purpose

Verify that the TTS conversion works correctly with a small file.

**Key points:**
- The system should convert markdown to audio
- It should strip formatting properly
- Output should be MP3 format

This test demonstrates the functionality without requiring a large document.
