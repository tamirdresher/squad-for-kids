# Area: API

> **Schema version:** 1.0 — Layer 1 monorepo context file.
> See `.squad/docs/monorepo-support.md` for full guide.

**Owner:** Data  
**Label:** `area:api`  
**Primary path:** `api/`  
**Related issue:** #1289

## What This Area Does

The API area contains the **FedRamp Dashboard ASP.NET Core Web API** — a .NET backend that
exposes compliance, controls, environments, history, and reports data for the FedRamp Dashboard.

Key responsibilities:
- **Authentication & authorization** via Azure AD / Entra ID (MSAL / `Microsoft.Identity.Web`)
  with RBAC role policies (`SecurityAdmin`, `SecurityEngineer`, `SRE`, `OpsViewer`)
- **PoP Token validation** (MISE V2) — `PopTokenValidationMiddleware` enforces Proof-of-Possession
  tokens on all authenticated endpoints
- **Data access** via Azure Cosmos DB (`CosmosDbService`) and Azure Log Analytics
  (`LogAnalyticsService`)
- **Cache telemetry** — `CacheTelemetryMiddleware` + `CacheTelemetryService` track cache hit/miss
  metrics
- **OpenAPI spec** — `openapi-fedramp-dashboard.yaml` at the area root

This API is the primary data backend for the FedRamp compliance dashboard UI.

## Key Files

| File | Purpose |
|------|---------|
| `api/FedRampDashboard.Api/Program.cs` | App startup, DI registration, auth pipeline |
| `api/FedRampDashboard.Api/FedRampDashboard.Api.csproj` | Project file — NuGet dependencies |
| `api/openapi-fedramp-dashboard.yaml` | OpenAPI 3.x contract |
| `api/FedRampDashboard.Api/Authorization/PopTokenValidationService.cs` | MISE V2 PoP token logic |
| `api/FedRampDashboard.Api/Authorization/RbacRoles.cs` | Role constants |
| `api/FedRampDashboard.Api/Controllers/ComplianceController.cs` | Compliance data endpoints |
| `api/FedRampDashboard.Api/Controllers/ControlsController.cs` | Controls endpoints |
| `api/FedRampDashboard.Api/Controllers/EnvironmentsController.cs` | Environment endpoints |
| `api/FedRampDashboard.Api/Controllers/HistoryController.cs` | Audit history endpoints |
| `api/FedRampDashboard.Api/Controllers/ReportsController.cs` | Reports & export endpoints |
| `api/FedRampDashboard.Api/Services/CosmosDbService.cs` | Cosmos DB data access |
| `api/FedRampDashboard.Api/Services/LogAnalyticsService.cs` | Azure Monitor / Log Analytics |
| `api/FedRampDashboard.Api/Middleware/PopTokenValidationMiddleware.cs` | PoP token middleware |
| `api/FedRampDashboard.Api/Middleware/CacheTelemetryMiddleware.cs` | Cache metrics middleware |
| `api/FedRampDashboard.Api/appsettings.json` | Config (AzureAd, Cosmos, PopToken sections) |

## Routing Hints

- **Auth / PoP token changes** → Data implements; Worf mandatory review
- **RBAC policy changes** (`RbacRoles.cs`, auth policies in `Program.cs`) → Data + Worf
- **New controllers or endpoints** → Data implements; update `openapi-fedramp-dashboard.yaml`
- **CosmosDB schema or query changes** → Data; check for index/RU impact
- **`appsettings.json` changes** → Data; never commit real secrets — use managed identity / Key Vault refs
- **Infrastructure wiring** (new services, container config) → B'Elanna for deployment review
- **OpenAPI contract changes** → Data authors; Picard signs off on breaking changes

## Constraints

- All endpoints must be covered by an RBAC `[Authorize(Policy = "...")]` attribute — no unauthenticated routes
- PoP token validation is mandatory for authenticated endpoints; do not bypass `PopTokenValidationMiddleware`
- Cosmos DB partition key design must be reviewed before any schema migration
- Never log PII or credential material — scrub before any diagnostic output
- The OpenAPI spec (`openapi-fedramp-dashboard.yaml`) must stay in sync with controller surfaces

## Conventions

- .NET 8 / ASP.NET Core minimal hosting model (`WebApplication.CreateBuilder`)
- Dependency injection throughout — no service locator anti-pattern
- Services follow interface + implementation pattern (e.g., `ICacheTelemetryService` / `CacheTelemetryService`)
- `appsettings.json` section names match `Options` class names (e.g., `CacheTelemetryOptions`)
- All new services registered in `Program.cs` — no auto-discovery

## Related

- `.squad/routing.md` — full routing table and label schema
- `.squad/docs/monorepo-support.md` — monorepo Layer 1–3 guide
- Issue #1289 — Layer 1 monorepo adoption
- `infrastructure/.squad-context.md` — cluster and Helm chart context for deploying this API
- `MISE_V2_POP_TOKEN_IMPLEMENTATION.md` — PoP token implementation reference (repo root)
