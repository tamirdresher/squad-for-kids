name: Squad Issue Notification

on:
  issues:
    types: [closed]

jobs:
  notify-teams:
    runs-on: self-hosted
    permissions:
      issues: read
      contents: read
    
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Get issue details
        id: issue
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        with:
          retries: 3
          retry-exempt-status-codes: 400,401,403,404,422
          script: |
            const issue = context.payload.issue;
            const comments = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue.number,
              per_page: 100
            });
            
            let lastComment = '';
            if (comments.data.length > 0) {
              lastComment = comments.data[comments.data.length - 1].body.substring(0, 500);
            }
            
            // Check for squad agent mentions
            let agent = 'Unknown';
            const agentMatch = lastComment.match(/(Picard|Data|B\\'Elanna|Seven|Worf)/i);
            if (agentMatch) {
              agent = agentMatch[1];
            } else if (issue.closed_by) {
              agent = issue.closed_by.login;
            }
            
            core.setOutput('title', issue.title);
            core.setOutput('number', issue.number);
            core.setOutput('url', issue.html_url);
            core.setOutput('agent', agent);
            core.setOutput('summary', lastComment || 'No additional details provided');
            core.setOutput('closed_by', issue.closed_by?.login || 'Unknown');

      - name: Send Teams notification
        if: env.TEAMS_WEBHOOK_URL != ''
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        env:
          TEAMS_WEBHOOK_URL: ${{ secrets.SQUAD_WEBHOOK_WINS || secrets.TEAMS_WEBHOOK_URL }}
          ISSUE_NUMBER: ${{ steps.issue.outputs.number }}
          ISSUE_TITLE: ${{ steps.issue.outputs.title }}
          ISSUE_URL: ${{ steps.issue.outputs.url }}
          CLOSED_BY: ${{ steps.issue.outputs.closed_by }}
          AGENT: ${{ steps.issue.outputs.agent }}
          SUMMARY: ${{ steps.issue.outputs.summary }}
        with:
          retries: 3
          retry-exempt-status-codes: 400,401,403,404,422
          script: |
            const payload = {
              type: "message",
              attachments: [{
                contentType: "application/vnd.microsoft.card.adaptive",
                content: {
                  type: "AdaptiveCard",
                  "$schema": "http://adaptivecards.io/schemas/adaptive-card.json",
                  version: "1.4",
                  body: [
                    { type: "TextBlock", text: "Issue Closed", weight: "Bolder", size: "Large", color: "Good" },
                    { type: "FactSet", facts: [
                      { title: "Issue:", value: `#${process.env.ISSUE_NUMBER} - ${process.env.ISSUE_TITLE}` },
                      { title: "Closed by:", value: process.env.CLOSED_BY },
                      { title: "Agent:", value: process.env.AGENT }
                    ]},
                    { type: "TextBlock", text: "Summary", weight: "Bolder", spacing: "Medium" },
                    { type: "TextBlock", text: process.env.SUMMARY, wrap: true, maxLines: 5 }
                  ],
                  actions: [
                    { type: "Action.OpenUrl", title: "View Issue", url: process.env.ISSUE_URL }
                  ]
                }
              }]
            };
            await fetch(process.env.TEAMS_WEBHOOK_URL, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify(payload)
            });
