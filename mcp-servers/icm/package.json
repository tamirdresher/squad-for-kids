{
  "name": "@mcp-servers/icm",
  "version": "1.0.0",
  "description": "MCP server for ICM (Incident Management) — search, view, and update incidents via the ICM REST API.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "icm-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@azure/identity": "^4.0.0",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
